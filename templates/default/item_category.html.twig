

        <!-- category -->
        <div class="p-b-55">
            <div class="how2 how2-cl4 flex-s-c m-b-30">
                <h3 class="f1-m-2 cl3 tab01-title">
                    热门信息
                </h3>
            </div>
            <div class="flex-wr-s-s m-rl--5">
                {% for category in categories %}
                <a href="{{ path('category_show',{'id':category.id}) }}" class="flex-c-c size-h-2 bo-1-rad-20 bocl12 f1-s-1 cl8 hov-btn2 trans-03 p-rl-20 p-tb-5 m-all-5">
                    {{ category.name }}
                </a>
                {% else %}
                    没有内容
                {% endfor %}
            </div>

        </div>

