{% extends 'base.html.twig' %}

{% block title %}Meta{% endblock %}

{% block body %}
    <h1>Meta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ metum.name }}</td>
            </tr>
            <tr>
                <th>Codes</th>
                <td>{{ metum.codes }}</td>
            </tr>
            <tr>
                <th>Enable</th>
                <td>{{ metum.enable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('meta_index') }}">back to list</a>

    <a href="{{ path('meta_edit', {'id': metum.id}) }}">edit</a>

    {{ include('meta/_delete_form.html.twig') }}
{% endblock %}
