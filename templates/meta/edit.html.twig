{% extends 'base.html.twig' %}

{% block title %}Edit Meta{% endblock %}

{% block body %}
    <h1>Edit Meta</h1>

    {{ include('meta/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('meta_index') }}">back to list</a>

    {{ include('meta/_delete_form.html.twig') }}
{% endblock %}
